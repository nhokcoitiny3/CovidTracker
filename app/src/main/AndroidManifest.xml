<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tiny.covidtracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <application
        android:name=".main.CovidTrackerApp"
        android:icon="@drawable/ic_virus2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_virus2"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.main.MainActivity" />
        <activity android:name=".ui.activities.main.SelectCountryActivity" />
        <activity android:name=".ui.activities.main.ProvinceActivity" />
        <activity android:name=".ui.activities.main.BrowserActivity" />
        <activity
            android:name=".ui.activities.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>